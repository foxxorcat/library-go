package steganography_test

import (
	"testing"
)

func Test(t *testing.T) {
	// var png steganography.SimplePNG
	// r := strings.NewReader(randomutils.RandomASCII(1024 * 1024))

	// bytes, _ := io.ReadAll(png.Encode(r, 1024*1024, false, 2))
	// os.WriteFile("./p.png", bytes, 0777)
}
